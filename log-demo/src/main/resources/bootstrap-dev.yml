eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://zbw:123456@localhost:6001/eureka

spring:
  cloud:
    config:
      profile: dev
      label: dev
